# S6 round-8: replace CORAL with lightweight DANN-style multi-domain adaptation.

missingness:
  gap_window: 6
  features: ["mask", "gap_length", "density_change"]

causal:
  method: t_learner
  treatment_proxy: vasopressor_proxy
  treatment_horizon: 24
  outcome_col: mortality_inhospital
  stability_gate:
    enabled: true
    max_std: 0.12
    max_abs_q90: 0.15

imputation:
  enabled: true
  fit_patients: 1024
  epochs: 1
  batch_size: 64
  n_layers: 2
  d_model: 64
  n_heads: 4
  d_ffn: 128
  patience: 1
  device: cpu

timesfm:
  enabled: true
  context_len: 32
  horizon_len: 6
  batch_size: 128
  backend: cpu
  freq: 0
  input_source: imputed
  selected_features: ["map", "heart_rate", "lactate", "creatinine"]
  repo_id: google/timesfm-1.0-200m-pytorch

dowhy:
  enabled: true
  max_samples: 3000
  random_common_cause_simulations: 20
  placebo_treatment_simulations: 20

domain_adaptation:
  enabled: true
  method: dann
  min_group_size: 500
  alpha: 1.0
  device: cpu
  epochs: 18
  patience: 4
  batch_size: 256
  hidden_dim: 64
  embedding_dim: 24
  lr: 0.001
  weight_decay: 0.0001
  dropout: 0.1
  lambda_domain: 0.3
  lambda_recon: 1.0
  warmup_epochs: 5
  val_fraction: 0.15

organ_scoring:
  horizon: 24
  sofa_version: sepsis3

phenotype_naming:
  deterioration_slope_threshold: 0.01
  cate_responsiveness_threshold: 0.02
  low_severity_sofa_max: 2
  low_severity_mortality_max: 0.10
  severity_split_targets:
    - hemodynamic_unstable_proxy_refractory
    - hemodynamic_unstable_proxy_responsive
    - neurological_decline
    - respiratory_failure

paths:
  s0_dir: data/s0
  s2_dir: data/s2
  s15_encoder: data/s15/checkpoints/pretrain_best.pt
  output_dir: data/s6_rerun_20260401_round8_dann

runtime:
  device: auto
  log_level: INFO
  seed: 42
