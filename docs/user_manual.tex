\documentclass[11pt,a4paper]{article}

% ---- Packages ----
\usepackage[margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{hyperref}
\usepackage{float}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{tabularx}
\usepackage{titlesec}
\usepackage{parskip}

% ---- Page style ----
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small Software User Manual}
\fancyhead[R]{\small ICU Sepsis Temporal Phenotyping}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

% ---- Code listing style ----
\lstset{
  language=bash,
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  backgroundcolor=\color{gray!5},
  columns=fullflexible,
}

% ---- Graphics path ----
\graphicspath{{figures/}}

\title{%
  \Large\bfseries Software User Manual\\[6pt]
  \large ICU Sepsis Temporal Phenotype Trajectory Analysis System
}
\author{Wang Ruike}
\date{\today}

\begin{document}

\maketitle
\tableofcontents
\newpage

% ==============================================================================
\section{Software Overview}
% ==============================================================================

This software implements a three-stage computational framework for self-supervised temporal phenotype trajectory analysis of ICU sepsis patients. Applied to the PhysioNet 2012 multi-center ICU database (11,986 patients), the system performs:

\begin{itemize}[nosep]
  \item \textbf{Stage 0}: Multi-center data extraction, preprocessing (forward fill, median imputation, outlier clipping, Z-score normalization), and cross-center split generation
  \item \textbf{Stage 1.5}: Self-supervised Transformer encoder training via masked value prediction + temporal contrastive learning
  \item \textbf{Stage 2--3}: Rolling-window embedding extraction, temporal clustering, and descriptive phenotype trajectory analysis
  \item \textbf{Downstream}: Leakage-aware OOF stacking mortality classifier with probability calibration optimization
  \item \textbf{External Transfer}: Frozen-transfer temporal analysis on MIMIC-IV and eICU-CRD
\end{itemize}

Key capabilities:

\begin{itemize}[nosep]
  \item Self-supervised representation learning from sparse, multi-center ICU time-series with explicit missingness modeling
  \item Descriptive temporal phenotype trajectories revealing within-stay dynamics (35.2\% of patients undergo phenotype transitions)
  \item Calibrated mortality prediction (Brier 0.090, ECE 0.023, AUROC 0.873, Recall 83.8\%)
  \item Cross-center validation and external database transfer
\end{itemize}

% ==============================================================================
\section{System Requirements}
% ==============================================================================

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{Item} & \textbf{Requirement} \\
\midrule
Operating System & macOS / Linux / Windows \\
Python Version   & 3.9 or above (tested with 3.14) \\
Memory           & 8 GB minimum (16 GB recommended for external cohorts) \\
Disk Space       & 2 GB minimum (including dependencies and data) \\
GPU              & Optional (accelerates Stage 1.5 pretraining; CPU works) \\
\bottomrule
\end{tabularx}
\end{table}

% ==============================================================================
\section{Installation Steps}
% ==============================================================================

\subsection{Obtain the Project Code}

\begin{lstlisting}
git clone https://github.com/ExusiaiHY/sepsis-phenotype-trajectories.git
cd sepsis-phenotype-trajectories
\end{lstlisting}

\subsection{Install Python Dependencies}

\begin{lstlisting}
pip install -r requirements.txt
\end{lstlisting}

Key dependencies:

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{Package} & \textbf{Purpose} \\
\midrule
numpy, pandas, scipy & Numerical computation and data processing \\
scikit-learn         & Clustering, classification, evaluation \\
torch (PyTorch)      & Transformer encoder training \\
pyyaml               & Configuration file parsing \\
matplotlib           & Visualization \\
lifelines            & Survival analysis (optional) \\
umap-learn           & Legacy V1 dimensionality reduction (optional) \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Verify Installation}

\begin{lstlisting}
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s0_smoke_test.py
\end{lstlisting}

% ==============================================================================
\section{Project Directory Structure}
% ==============================================================================

\begin{lstlisting}[language={},basicstyle=\ttfamily\footnotesize]
project/
|-- config/                     # YAML configuration files
|   |-- s0_config.yaml          # Data extraction & preprocessing
|   |-- s1_config.yaml          # Masked pretraining (Stage 1)
|   |-- s15_config.yaml         # Contrastive pretraining (Stage 1.5)
|   |-- s15_trainval_config.yaml # S1.5 with trainval paths
|   |-- s2_config.yaml          # Rolling window & temporal clustering
|   +-- config.yaml             # Legacy V1 pipeline config
|-- s0/                         # Stage 0: Data pipeline
|   |-- physionet2012_extractor.py   # PhysioNet 2012 data extraction
|   |-- preprocessor.py              # Forward fill, median, clip, Z-score
|   |-- schema.py                    # 21-feature + proxy + static schema
|   |-- splits.py                    # Cross-center and random splits
|   |-- external_temporal_builder.py # MIMIC-IV / eICU to S0 format
|   +-- dataset.py                   # PyTorch Dataset wrapper
|-- s1/                         # Stage 1: Masked pretraining
|   |-- encoder.py              # ICUTransformerEncoder (2-layer, 4-head, d=128)
|   |-- pretrain.py             # Masked value prediction training
|   +-- extract_embeddings.py   # Patient embedding extraction
|-- s15/                        # Stage 1.5: Contrastive + downstream
|   |-- contrastive_encoder.py  # NT-Xent + projection head
|   |-- pretrain_contrastive.py # Combined masked + contrastive training
|   |-- classification_eval.py  # Frozen embedding classifier
|   |-- advanced_classifier.py  # Multi-view HGB/LR classifiers
|   |-- stacking_classifier.py  # OOF stacking mortality classifier
|   |-- stacking_validation.py  # Bootstrap CI + calibration analysis
|   |-- calibration.py          # Post-hoc calibration methods (5 calibrators)
|   |-- calibrated_stacking.py  # Calibration-aware stacking classifier
|   |-- calibration_losses.py   # FocalLoss, BrierLoss, SoftECELoss
|   |-- finetune_supervised.py  # End-to-end attention-pooled fine-tuning
|   +-- sepsis2019_bridge.py    # PhysioNet 2019 auxiliary bridge
|-- s2light/                    # Stage 2-3: Temporal analysis
|   |-- rolling_embeddings.py   # Rolling-window embedding extraction
|   |-- temporal_clustering.py  # Per-window KMeans clustering
|   |-- transition_analysis.py  # Trajectory classification & transitions
|   +-- visualization.py        # Sankey diagrams, prevalence plots
|-- scripts/                    # Entry-point runner scripts
|   |-- s0_prepare.py           # Prepare PhysioNet 2012 data
|   |-- s1_pretrain.py          # Stage 1 masked pretraining
|   |-- s15_pretrain.py         # Stage 1.5 contrastive pretraining
|   |-- s15_extract.py          # Extract S1.5 embeddings
|   |-- s15_train_stacking_classifier.py     # Train OOF stacking
|   |-- s15_validate_stacking_classifier.py  # Validate + calibration
|   |-- s15_calibrate.py                     # Post-hoc calibration
|   |-- s15_train_calibrated_stacking.py     # Train calibrated model
|   |-- s15_calibration_hparam_search.py     # Hparam search (30 configs)
|   |-- s15_calibration_comparison.py        # 8-model comparison
|   |-- s2_extract_rolling.py   # Extract rolling-window embeddings
|   |-- s2_cluster_and_analyze.py # Temporal clustering + analysis
|   |-- s3_cross_center_validation.py # Cross-center validation
|   +-- run_external_temporal_stage3.py # External temporal transfer
|-- src/                        # Legacy V1 static pipeline
|   +-- main.py                 # Legacy entry point (simulated/MIMIC/eICU)
|-- docs/                       # Documentation
|-- data/                       # Data artifacts (gitignored for large files)
|-- outputs/                    # Generated figures and reports
+-- tests/                      # Pytest test suite
\end{lstlisting}

% ==============================================================================
\section{Input Data Format}
% ==============================================================================

\subsection{PhysioNet 2012 (Primary)}

Place the PhysioNet 2012 data files in \texttt{data/raw\_aligned/}:
\begin{itemize}[nosep]
  \item \texttt{set-a/}, \texttt{set-b/}, \texttt{set-c/} directories with patient \texttt{.txt} files
  \item \texttt{Outcomes-a.txt}, \texttt{Outcomes-b.txt}, \texttt{Outcomes-c.txt}
\end{itemize}

Run data preparation:
\begin{lstlisting}
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s0_prepare.py
\end{lstlisting}

This produces:
\begin{itemize}[nosep]
  \item \texttt{data/s0/processed/continuous.npy} -- (11986, 48, 21) preprocessed tensor
  \item \texttt{data/s0/processed/masks\_continuous.npy} -- (11986, 48, 21) observation masks
  \item \texttt{data/s0/processed/proxy\_indicators.npy} -- (11986, 48, 2) proxy features
  \item \texttt{data/s0/static.csv} -- Patient demographics + mortality labels
  \item \texttt{data/s0/splits.json} -- Train/val/test indices (cross-center split)
\end{itemize}

\subsection{External Databases (Supplementary)}

For MIMIC-IV or eICU frozen-transfer analysis:
\begin{lstlisting}
python3 scripts/run_external_temporal_stage3.py --source mimic --device auto
python3 scripts/run_external_temporal_stage3.py --source eicu --device auto
\end{lstlisting}

% ==============================================================================
\section{Configuration Files}
% ==============================================================================

All configuration is in \texttt{config/} using YAML format.

\subsection{Encoder Configuration (\texttt{s15\_config.yaml})}

\begin{lstlisting}[language={}]
encoder:
  n_features: 21       # Input feature channels
  d_model: 128          # Transformer hidden dimension
  n_heads: 4            # Attention heads
  n_layers: 2           # Transformer layers
  d_ff: 256             # Feed-forward dimension
  dropout: 0.2
  max_seq_len: 48       # Hours in observation window

contrastive:
  view_len: 30          # Stochastic window length (hours)
  mask_ratio: 0.15      # Masked value prediction ratio
  temperature: 0.1      # NT-Xent temperature
  proj_dim: 64          # Projection head output
  max_lambda: 0.5       # Maximum contrastive weight
  warmup_epochs: 10     # Lambda warmup period

pretraining:
  epochs: 50
  batch_size: 64
  lr: 1.0e-3
  weight_decay: 1.0e-5
  patience: 15
  grad_clip: 1.0
  seed: 42
\end{lstlisting}

\subsection{Rolling Window Configuration (\texttt{s2\_config.yaml})}

\begin{lstlisting}[language={}]
rolling:
  window_len: 24        # Window size in hours
  stride: 6             # Stride between windows
  seq_len: 48           # Total sequence length

clustering:
  k: 4                  # Number of temporal phenotypes
\end{lstlisting}

% ==============================================================================
\section{How to Run}
% ==============================================================================

\textbf{Important}: Always prefix Python commands with:
\begin{lstlisting}
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE
\end{lstlisting}

\subsection{Full Pipeline (Stages 0 to 3)}

\begin{lstlisting}
cd project

# Stage 0: Data preparation
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s0_prepare.py

# Stage 1.5: Self-supervised pretraining
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s15_pretrain.py

# Extract embeddings
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s15_extract.py

# Stage 2: Rolling-window embeddings
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s2_extract_rolling.py

# Stage 3: Temporal clustering + trajectory analysis
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s2_cluster_and_analyze.py

# Cross-center validation
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s3_cross_center_validation.py
\end{lstlisting}

\subsection{Mortality Prediction with Calibration}

\begin{lstlisting}
# Train OOF stacking classifier
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE \
  python3 scripts/s15_train_stacking_classifier.py

# Validate with bootstrap CIs and calibration analysis
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE \
  python3 scripts/s15_validate_stacking_classifier.py \
    --model-dir data/s15_trainval/stacking_accuracy

# Post-hoc calibration (applies 5 methods to existing model)
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE \
  python3 scripts/s15_calibrate.py \
    --model-dir data/s15_trainval/stacking_accuracy

# Train calibrated stacking model (recommended)
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE \
  python3 scripts/s15_train_calibrated_stacking.py

# Run hyperparameter search for calibration optimization
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE \
  python3 scripts/s15_calibration_hparam_search.py

# Full 8-model comparison report
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE \
  python3 scripts/s15_calibration_comparison.py
\end{lstlisting}

\subsection{External Temporal Transfer}

\begin{lstlisting}
# Run frozen S1.5 + Stage 3 on MIMIC-IV and eICU
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE \
  python3 scripts/run_external_temporal_stage3.py --source all --device auto
\end{lstlisting}

\subsection{Legacy V1 Pipeline (Simulated Data)}

\begin{lstlisting}
cd project
python src/main.py --n-patients 500 --method kmeans
\end{lstlisting}

% ==============================================================================
\section{Output Description}
% ==============================================================================

\subsection{Stage 1.5 Outputs (\texttt{data/s15\_trainval/})}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{File} & \textbf{Description} \\
\midrule
\texttt{checkpoints/pretrain\_best.pt} & Best pretraining checkpoint \\
\texttt{embeddings\_s15.npy}           & Patient embeddings (11986, 128) \\
\texttt{pretrain\_log.json}            & Training loss history \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Stacking Classifier Outputs (\texttt{data/s15\_trainval/stacking\_accuracy/})}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{File} & \textbf{Description} \\
\midrule
\texttt{stacking\_mortality\_classifier.pkl}         & Trained model bundle \\
\texttt{stacking\_mortality\_classifier\_report.json} & Training report with operating points \\
\texttt{stacking\_validation\_report.json}            & Bootstrap CIs + calibration + importance \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Calibration Outputs}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{Directory} & \textbf{Key Files} \\
\midrule
\texttt{stacking\_accuracy/calibration/} & \texttt{calibration\_report.json}, \texttt{calibrators.pkl}, \texttt{test\_probs\_*.npy} \\
\texttt{calibrated\_stacking/}           & \texttt{calibrated\_stacking\_classifier.pkl}, \texttt{calibrated\_stacking\_report.json} \\
\texttt{calibration\_hparam\_search/}    & \texttt{hparam\_search\_report.json} \\
\texttt{calibration\_comparison/}        & \texttt{calibration\_comparison\_report.json} \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Temporal Analysis Outputs (\texttt{data/s2light/})}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{File} & \textbf{Description} \\
\midrule
\texttt{rolling\_embeddings.npy}   & (11986, 5, 128) per-window embeddings \\
\texttt{cluster\_assignments.npy}  & (11986, 5) per-window cluster labels \\
\texttt{trajectory\_stats.json}    & Stability, transitions, mortality stratification \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Visualization Outputs (\texttt{outputs/figures/})}

\begin{table}[H]
\centering
\begin{tabularx}{\textwidth}{lX}
\toprule
\textbf{File} & \textbf{Description} \\
\midrule
\texttt{per\_window\_prevalence.png}   & Phenotype prevalence across rolling windows \\
\texttt{sankey\_transitions.png}       & Phenotype transition flow diagram \\
\texttt{mortality\_by\_trajectory.png} & Mortality rates by trajectory category \\
\texttt{pipeline\_diagram.png}         & Three-stage framework architecture \\
\bottomrule
\end{tabularx}
\end{table}

% ==============================================================================
\section{Frequently Asked Questions}
% ==============================================================================

\textbf{Q1: ``ModuleNotFoundError'' occurs at runtime}

A: Install all dependencies: \texttt{pip install -r requirements.txt}. For PyTorch, follow \url{https://pytorch.org} for your platform.

\bigskip

\textbf{Q2: Pretraining is slow on CPU}

A: Stage 1.5 pretraining benefits from GPU acceleration. On CPU it takes about 25 min, vs about 8 min on GPU. Use \texttt{--device cpu} or \texttt{--device cuda} to control.

\bigskip

\textbf{Q3: How to reproduce the calibration optimization?}

A: Run the full calibration pipeline:
\begin{lstlisting}
# 1. Train original stacking model (if not already done)
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s15_train_stacking_classifier.py

# 2. Apply post-hoc calibration to original model
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s15_calibrate.py --model-dir data/s15_trainval/stacking_accuracy

# 3. Train calibrated stacking model (structural fix)
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s15_train_calibrated_stacking.py

# 4. Compare all methods
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE python3 scripts/s15_calibration_comparison.py
\end{lstlisting}

\bigskip

\textbf{Q4: How to use a custom mortality prior for Bayesian calibration?}

A: Pass \texttt{--prior-rate 0.20} to \texttt{scripts/s15\_calibrate.py} to use 20\% instead of 14.2\%.

\bigskip

\textbf{Q5: What threshold should I use for clinical deployment?}

A: The calibrated model supports multiple operating points:
\begin{itemize}[nosep]
  \item \textbf{Triage (high sensitivity)}: threshold=0.05, captures >90\% of deaths
  \item \textbf{Balanced}: threshold=0.09, recall 83.8\%, balanced accuracy 80.1\%
  \item \textbf{Resource allocation (high specificity)}: threshold=0.30, precision approximately 60\%
\end{itemize}

\bigskip

\textbf{Q6: Results differ between runs}

A: Ensure \texttt{seed: 42} in config files. All random processes use deterministic seeding.

\bigskip

\textbf{Q7: How to run external temporal transfer on MIMIC-IV / eICU?}

A: First prepare the source-specific data with \texttt{src/main.py}, then:
\begin{lstlisting}
OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE \
  python3 scripts/run_external_temporal_stage3.py --source all --device auto
\end{lstlisting}

% ==============================================================================
\section{Important Notes}
% ==============================================================================

\begin{enumerate}[nosep]
  \item Always use \texttt{OMP\_NUM\_THREADS=1 KMP\_DUPLICATE\_LIB\_OK=TRUE} prefix for Python commands to avoid threading conflicts
  \item The calibrated stacking model (\texttt{data/s15\_trainval/calibrated\_stacking/}) is the recommended model for clinical applications
  \item All mortality outcomes use verified PhysioNet Outcomes files (14.2\% in-hospital mortality rate)
  \item External transfer results (MIMIC-IV, eICU) use the frozen PhysioNet-trained encoder and should be interpreted as supplementary frozen-transfer analyses
  \item Model artifacts use serialized format for sklearn compatibility -- only load model files produced by this project
  \item Large data files (\texttt{.npy}, \texttt{.pt}) are gitignored; re-run the pipeline to regenerate them
\end{enumerate}

\end{document}
